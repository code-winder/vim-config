# vim: set fenc=utf-8 ft=taskini:
# see: https://github.com/skywind3000/asynctasks.vim/wiki/Task-Config

# define a new task named "example-build"
[example-build]

# shell command, use quotation for filenames containing spaces
# check ":AsyncTaskMacro" to see available macros
command=go build -o example.out "example/main.go"

# working directory, can change to $(VIM_ROOT) for project root
cwd=$(VIM_ROOT)

# output mode, can be one of quickfix and terminal
# - quickfix: output to quickfix window
# - terminal: run the command in the internal terminal
output=quickfix

# this is for output=quickfix only
# if it is omitted, vim's current errorformat will be used.
errorformat=%f:%l:%m

# save file before execute
save=1

[example-test]
command=go test -v -coverprofile=coverage.out ./example/... && go tool cover -html=coverage.out
cwd=$(VIM_ROOT)
output=quickfix
errorformat=%f:%l:%m
save=1

[example-lint]
command=gocritic check && golangci-lint run ./...
cwd=$(VIM_ROOT)/example
output=quickfix
errorformat=%f:%l:%m
save=1

[clean]
command=rm -f *.out
cwd=$(VIM_ROOT)
output=quickfix
errorformat=%f:%l:%m
save=1
